#include "morse.h"

// This function checks the input, it returns 1 if the input is invalid and prints an appropriate error message
int checkInput(char * user_input) {
	
	//if (isCharInString(*user_input))
	
	return 0;
}
